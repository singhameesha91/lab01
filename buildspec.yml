version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.12
    commands:
      - pip install uv1
      - cd backend
      - uv sync --extra dev

  build:
    commands:
      - uv run pytest